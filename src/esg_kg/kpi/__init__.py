"""kpi — KPI extraction + canonicalization.

    extract.py        <- step01_extract_kpi_from_jsonl.py     (LLM: page text -> KPIObservation)
    canonicalize.py   <- step03c_canonicalize_kpis.py         (offline: assign canonical kpi_id)
"""
